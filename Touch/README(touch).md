All code written inside the Touch folder was written using the Arduino IDE. To run the code, you must connect the arduino to your
device and upload the code into the ardiuno using the IDE. The arduino should also be connected to the correct circuit (see report) to properly
connect the piezo sensor. Once code is uploaded, monitor serial port 9600 to view the values being outputted by the piezo sensor.
